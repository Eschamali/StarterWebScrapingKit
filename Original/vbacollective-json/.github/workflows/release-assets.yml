name: Release Assets

on:
  release:
    types:
      - published

permissions:
  contents: write

jobs:
  upload-json-module:
    name: Upload JSON.cls
    runs-on: ubuntu-latest

    steps:
      - name: Check out release tag
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name }}

      - name: Upload JSON.cls to release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: gh release upload "$RELEASE_TAG" package/JSON.cls --clobber
